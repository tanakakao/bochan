name: Fix single-output hybrid acquisition routing

on:
  pull_request:
    branches:
      - agent/single-output-hybrid-routing-runner

permissions:
  contents: write

jobs:
  patch-and-validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.ref }}
          token: ${{ secrets.GITHUB_TOKEN }}
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: pip
      - name: Apply source patch
        run: python scripts/apply_single_output_hybrid_acquisition_routing.py
      - name: Commit patched source
        shell: bash
        run: |
          if git diff --quiet -- src/bochan/api/engine.py src/bochan/api/__init__.py; then
            echo "No source changes to commit"
          else
            git config user.name "github-actions[bot]"
            git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
            git add src/bochan/api/engine.py src/bochan/api/__init__.py
            git commit -m "Fix single-output hybrid acquisition routing"
            git push origin HEAD:${{ github.event.pull_request.head.ref }}
          fi
      - name: Install package and test tools
        run: |
          python -m pip install --upgrade pip
          python -m pip install -e ".[web]" pytest ruff
      - name: Ruff new and updated tests
        run: |
          python -m ruff format --check tests/test_single_output_hybrid_acquisition_routing.py tests/test_webapp_single_objective_nipv.py
          python -m ruff check tests/test_single_output_hybrid_acquisition_routing.py tests/test_webapp_single_objective_nipv.py
      - name: Compile routing source
        run: python -m py_compile src/bochan/api/engine.py src/bochan/api/__init__.py
      - name: Focused routing tests
        run: |
          python -m pytest -q tests/test_single_output_hybrid_acquisition_routing.py tests/test_webapp_single_objective_nipv.py
