name: MLIP FastAPI capabilities Phase 25 smoke

on:
  pull_request:
    paths:
      - "src/bochan/serving/fastapi/routers/material_capabilities.py"
      - "src/bochan/serving/fastapi/routers/__init__.py"
      - "src/bochan/serving/fastapi/schemas/material_capabilities.py"
      - "src/bochan/serving/fastapi/schemas/__init__.py"
      - "tests/serving/fastapi/test_material_capabilities_phase25.py"
      - ".github/workflows/mlip-fastapi-capabilities-phase25.yml"

jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Install test surface
        run: |
          python -m pip install --upgrade pip
          python -m pip install -e ".[test,api]"
          python -m pip install ruff
      - name: Run Phase 25 tests
        run: python -m pytest tests/serving/fastapi/test_material_capabilities_phase25.py -q
      - name: Ruff
        run: |
          ruff check \
            src/bochan/serving/fastapi/routers/material_capabilities.py \
            src/bochan/serving/fastapi/routers/__init__.py \
            src/bochan/serving/fastapi/schemas/material_capabilities.py \
            src/bochan/serving/fastapi/schemas/__init__.py \
            tests/serving/fastapi/test_material_capabilities_phase25.py
