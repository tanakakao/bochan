name: MLIP runtime relaxation Phase 28 smoke

on:
  pull_request:
    paths:
      - "src/bochan/serving/fastapi/routers/material_workflow.py"
      - "src/bochan/serving/fastapi/schemas/material_workflow.py"
      - "src/bochan/serving/fastapi/schemas/__init__.py"
      - "tests/serving/fastapi/test_material_workflow_phase28.py"
      - ".github/workflows/mlip-runtime-relaxation-phase28.yml"

jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Install test surface
        run: |
          python -m pip install --upgrade pip
          python -m pip install -e ".[test,api]"
          python -m pip install ruff
      - name: Run Phase 28 tests
        run: python -m pytest tests/serving/fastapi/test_material_workflow_phase28.py -q
      - name: Ruff
        run: |
          ruff check src/bochan/serving/fastapi/routers/material_workflow.py \
            src/bochan/serving/fastapi/schemas/material_workflow.py \
            src/bochan/serving/fastapi/schemas/__init__.py \
            tests/serving/fastapi/test_material_workflow_phase28.py
