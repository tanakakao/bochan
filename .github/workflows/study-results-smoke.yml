name: Study results smoke

on:
  pull_request:
    branches:
      - main
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
      - name: Install
        run: python -m pip install -e ".[test,api,visualization]"
      - name: Run Study result tests
        shell: bash
        run: |
          set +e
          pytest -q --maxfail=1 \
            tests/api/test_study_results.py \
            tests/test_study_visualization.py \
            tests/test_visualization_categorical_fixed_values.py \
            tests/test_fastapi_study_endpoints.py \
            2>&1 | tee pytest-study.log
          status=${PIPESTATUS[0]}
          echo "$status" > pytest-study.status
          exit "$status"
      - name: Run lint
        if: always()
        shell: bash
        run: |
          python -m pip install ruff
          set +e
          ruff check \
            --per-file-ignores "src/bochan/visualization/utils.py:B009" \
            src/bochan/api/study_results.py \
            src/bochan/visualization/categorical_axis.py \
            src/bochan/visualization/ordinal_display.py \
            src/bochan/visualization/study.py \
            src/bochan/visualization/utils.py \
            src/bochan/serving/fastapi/dependencies.py \
            src/bochan/serving/fastapi/router.py \
            src/bochan/serving/fastapi/routers/studies.py \
            src/bochan/serving/fastapi/schemas/study.py \
            src/bochan/serving/fastapi/stores/study.py \
            src/bochan/serving/fastapi/study_service.py \
            tests/api/test_study_results.py \
            tests/test_study_visualization.py \
            tests/test_visualization_categorical_fixed_values.py \
            tests/test_fastapi_study_endpoints.py \
            2>&1 | tee ruff-study.log
          status=${PIPESTATUS[0]}
          echo "$status" > ruff-study.status
          exit "$status"
      - name: Upload diagnostics
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: study-results-smoke-logs
          path: |
            pytest-study.log
            pytest-study.status
            ruff-study.log
            ruff-study.status
          if-no-files-found: warn
