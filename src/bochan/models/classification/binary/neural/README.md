# Binary neural models

Differentiable neural surrogate models for binary classification live in this package.

`deep_ensemble.py` exposes normal and mixed-input Deep Ensemble binary classifiers. Shared neural classification internals are private under `bochan.models.classification.common`.
