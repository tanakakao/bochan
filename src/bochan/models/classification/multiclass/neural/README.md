# Multiclass neural models

Differentiable neural surrogate models for multiclass classification live in this package.

`deep_ensemble.py` exposes normal and mixed-input Deep Ensemble multiclass classifiers. Shared neural classification internals are private under `bochan.models.classification.common`.
